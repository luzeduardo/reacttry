version: '2'
services:
  webpack:
    build:
      context: .
      dockerfile: Dockerfile
    image: webpack-react
    container_name: webpack-react-container
    ports:
      - "8000:8000"      
      - "3001:3001"
    volumes:
      - .:/usr/src/app     